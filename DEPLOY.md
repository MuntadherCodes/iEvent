# Deploying iEvent

Production deployment runbook — arrives with the ops-readiness phase; **do not deploy from this round**.

Until then: local/dev usage only, via `docker compose up -d --build app` (see [README.md](README.md)).
