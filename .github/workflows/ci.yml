name: CI

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-test:
    name: Build & unit/smoke tests
    runs-on: ubuntu-latest
    permissions:
      contents: write

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: ievent
          POSTGRES_USER: ievent
          POSTGRES_PASSWORD: ci-test-password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ievent -d ievent"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Build and test (mvn verify)
        id: verify
        shell: bash
        env:
          SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/ievent
          SPRING_DATASOURCE_USERNAME: ievent
          SPRING_DATASOURCE_PASSWORD: ci-test-password
          SEED_DEMO: 'true'
          RATE_LIMIT_ENABLED: 'false'
        run: |
          set -o pipefail
          mvn -B verify 2>&1 | tee mvn-output.log

      - name: Assemble plain-text report
        if: always()
        shell: bash
        run: |
          {
            echo "iEvent CI report — build-test — run ${{ github.run_number }}"
            echo "Commit:  ${{ github.sha }}"
            echo "Date:    $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
            echo "Outcome of 'mvn -B verify': ${{ steps.verify.outcome }}"
            echo
            echo "================ Surefire test summary ================"
            if compgen -G "target/surefire-reports/*.txt" > /dev/null; then
              cat target/surefire-reports/*.txt
            else
              echo "(no surefire report files found)"
            fi
            echo
            echo "======== Last 200 lines of build/test output =========="
            if [ -f mvn-output.log ]; then
              tail -n 200 mvn-output.log
            else
              echo "(no maven output captured)"
            fi
          } > report-full.txt
          # Release notes are capped at 125,000 characters by GitHub. Keep the
          # summary + FAILED test names + the tail; the complete file goes up
          # as a build artifact next to the surefire reports.
          {
            head -c 60000 report-full.txt
            echo
            echo "======== (report truncated for the release body; full report in the run artifacts) ========"
            echo
            echo "======== FAILED / ERROR test lines ========"
            grep -E "FAIL|ERROR\]|Tests run:.*Failures: [1-9]|Tests run:.*Errors: [1-9]" report-full.txt | head -n 200 || true
            echo
            echo "======== Last 120 lines ========"
            tail -n 120 report-full.txt || true
          } | head -c 110000 > report.txt || true
          echo "---- report.txt preview ----"
          head -n 40 report.txt

      - name: Publish report as release (tag ci-report)
        if: always()
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          gh release delete ci-report --yes || true
          git tag -f ci-report
          git push -f origin ci-report || echo "::warning::could not move the ci-report tag"
          # A failed publish must never turn a green build red (or hide a red one).
          gh release create ci-report \
            --title "CI report (build-test) · run ${{ github.run_number }} · ${{ steps.verify.outcome }}" \
            --notes-file report.txt \
            || echo "::warning::release publish failed; the report is still available as a run artifact"

      - name: Upload build report + surefire reports artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-test-report
          path: |
            report-full.txt
            target/surefire-reports/
          if-no-files-found: ignore

  compose-e2e:
    name: Compose stack & Playwright E2E
    runs-on: ubuntu-latest
    needs: build-test
    # Runs after build-test but regardless of its result, so a red smoke test
    # never leaves the e2e report silently frozen at an older commit.
    if: always()
    permissions:
      contents: write

    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Create .env from .env.example
        shell: bash
        run: |
          cp .env.example .env
          sed -i 's/^POSTGRES_PASSWORD=.*/POSTGRES_PASSWORD=ci-test-password/' .env
          sed -i 's/^SEED_DEMO=.*/SEED_DEMO=true/' .env
          sed -i 's/^SEED_SCALE=.*/SEED_SCALE=300/' .env
          echo 'RATE_LIMIT_ENABLED=false' >> .env
          grep -E '^(POSTGRES_PASSWORD|SEED_DEMO|SEED_SCALE|RATE_LIMIT_ENABLED)=' .env

      - name: Start the stack
        shell: bash
        run: docker compose up -d --build

      - name: Wait for app health
        shell: bash
        run: |
          echo "Waiting for http://localhost:8080/actuator/health to report UP …"
          for i in $(seq 1 120); do
            if curl -fsS http://localhost:8080/actuator/health 2>/dev/null | grep -q UP; then
              echo "App healthy after $i attempt(s)."
              exit 0
            fi
            if [ $((i % 12)) -eq 0 ]; then
              echo "--- still waiting (attempt $i/120) — recent app logs: ---"
              docker compose logs app --tail 50 || true
            fi
            sleep 5
          done
          echo "App never became healthy — full logs follow."
          docker compose logs
          exit 1

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: e2e/package-lock.json

      - name: Run Playwright walkthrough
        id: e2e
        shell: bash
        working-directory: e2e
        run: |
          set -o pipefail
          npm ci
          npx playwright install --with-deps chromium
          npx playwright test 2>&1 | tee ../e2e-output.log

      - name: Assemble plain-text report
        if: always()
        shell: bash
        run: |
          {
            echo "iEvent CI report — compose-e2e — run ${{ github.run_number }}"
            echo "Commit:  ${{ github.sha }}"
            echo "Date:    $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
            echo "Outcome of Playwright run: ${{ steps.e2e.outcome }}"
            echo
            echo "==================== E2E results ======================"
            if [ -f e2e-output.log ]; then
              cat e2e-output.log
            else
              echo "(no Playwright output captured)"
            fi
            echo
            echo "============ App logs (last 300 lines) ================"
            docker compose logs app --tail 300 || echo "(could not collect app logs)"
          } > report-full.txt
          {
            head -c 20000 report-full.txt
            echo
            echo "======== (report truncated for the release body; full report in the run artifacts) ========"
            echo
            echo "======== Failed tests ========"
            grep -nE "^\s+[0-9]+\) |✘|failed|Error:" e2e-output.log 2>/dev/null | head -n 200 || true
            echo
            echo "======== Playwright summary (last 60 lines of test output) ========"
            tail -n 60 e2e-output.log 2>/dev/null || true
            echo
            echo "======== App log (last 120 lines) ========"
            docker compose logs app --tail 120 2>/dev/null || true
          } | head -c 110000 > report.txt || true
          echo "---- report.txt preview ----"
          head -n 40 report.txt

      - name: Publish report as release (tag ci-report-compose)
        if: always()
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          gh release delete ci-report-compose --yes || true
          git tag -f ci-report-compose
          git push -f origin ci-report-compose || echo "::warning::could not move the ci-report-compose tag"
          gh release create ci-report-compose \
            --title "CI report (compose-e2e) · run ${{ github.run_number }} · ${{ steps.e2e.outcome }}" \
            --notes-file report.txt \
            || echo "::warning::release publish failed; the report is still available as a run artifact"
          shopt -s globstar nullglob
          screenshots=(e2e/test-results/**/*.png)
          if [ ${#screenshots[@]} -gt 0 ]; then
            echo "Attaching ${#screenshots[@]} failure screenshot(s) to the release"
            gh release upload ci-report-compose "${screenshots[@]}" --clobber
          else
            echo "No failure screenshots to attach."
          fi

      - name: Upload Playwright artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-results
          path: |
            report-full.txt
            e2e-output.log
            e2e/playwright-report/
            e2e/test-results/
          if-no-files-found: ignore
