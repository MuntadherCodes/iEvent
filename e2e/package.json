{
  "name": "ievent-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright end-to-end walkthrough for iEvent (runs against the docker-compose stack)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
